## Install Valhalla

script to install valhalla and keep up to date with daily checks at 4am.

```terminal
curl -fsSL https://raw.githubusercontent.com/NadK2/valhalla-install/refs/heads/main/install.sh | bash
```
